# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# Data & model artifacts
# NOTE: archive/BBC News Summary/ (the dataset actually used by data.py) IS
# tracked on purpose, so `git clone`/`git pull` in Google Colab brings the
# dataset with it - no manual upload step needed. Only the raw downloaded
# zip, the redundant duplicate-case copy of the same data, and
# generated/regenerable artifacts are ignored.
archive.zip
archive/bbc news summary/
*.npz
*.npy
*.pt
*.pth
*.ckpt

# OS/editor
.DS_Store

.claude

# Superseded by git clone/pull inside train.ipynb's Colab bootstrap cell
numpy_seq2seq_colab.zip
/numpy_seq2seq/train_log.txt